{
  "name": "valentines",
  "version": "1.0.0",
  "description": "Valentine link generator with status tracking",
  "private": true,
  "scripts": {
    "serve": "firebase serve",
    "deploy": "npm run build:config && firebase deploy",
    "build:config": "node scripts/generate-firebase-config.js",
    "postinstall": "node scripts/generate-firebase-config.js"
  },
  "devDependencies": {
    "dotenv": "^16.3.1"
  }
}